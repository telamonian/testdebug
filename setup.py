#!/usr/bin/env python
from setuptools import find_packages, setup

setup(
    author='Max Klein',
    description='Set up framework agnostic unit tests, that can be run using any testing framework.',
    license='UOISL',
    name='testag',
    packages=find_packages(),
)
